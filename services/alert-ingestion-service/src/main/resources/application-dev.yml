# Local, non-docker development — services resolved on localhost instead of the compose network.
spring:
  kafka.bootstrap-servers: localhost:29092
  data.mongodb.uri: mongodb://localhost:27017/alertdb
eureka.client.serviceUrl.defaultZone: http://localhost:8761/eureka
logging.level.io.incidentops: DEBUG

# Fixed demo secret for local/dev only — same value every other service's application-dev.yml
# uses, since JwtFilter here now needs to validate tokens too (guards the new read-only
# GET /api/webhooks/alerts* endpoints; HMAC-only ingestion is unaffected).
JWT_SECRET: GIEEsDn4DnEZdSKtFPJ3fZoVqGgD2z7Vl9LZk/b4So0=
